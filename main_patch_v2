# main.py 연결 참고 파일

from dotenv import load_dotenv
load_dotenv()

from smoke_test import quick_smoke_test

def run_startup_smoke():
    try:
        quick_smoke_test()
        print("✅ smoke test ok", flush=True)
    except Exception as e:
        print(f"❌ smoke test failed: {e}", flush=True)
